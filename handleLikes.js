// handleLikes.js

export function handleLike(imageId) {
    // Simulate liking the image by incrementing the like count
    return {
        imageId: imageId,
        userId: 1 // Assuming user ID
    };
}

